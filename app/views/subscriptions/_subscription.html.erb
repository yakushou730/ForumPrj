<% if subscription %>
  <%= link_to "Unsubscribe", unsubscribe_topic_path(@topic), :class => "btn btn-default", :method => :post, :remote => true %>
<% else %>
  <%= link_to "Subscribe", subscribe_topic_path(@topic), :class => "btn btn-default", :method => :post, :remote => true %>
<% end %>
